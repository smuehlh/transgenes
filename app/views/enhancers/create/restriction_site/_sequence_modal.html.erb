<a data-toggle="modal" href="#view-modal-<%= id %>" class="alert-link">Click to inspect parsed motifs.</a>
<div id="view-modal-<%= id %>" class="modal" role="dialog">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Restriction enzyme motifs to <%= @restriction_enzyme.name %></h4>
                <small class="text-muted">
                    <table class="padding-between-cols">
                        <tbody>
                            <tr>
                                <th scope="row">Number of motifs:</th>
                                <td><%= @restriction_enzyme.data.size %></td>
                            </tr>
                        </tbody>
                    </table>
                </small>
            </div>
            <div class="modal-body">
                <pre class="text-muted input-view-text"><%= @restriction_enzyme.data.join("\n") %></pre>
            </div>
        </div>
    </div>
</div>